# Baked in at build time from the ENDERMAN_GRIEF_TEST_MODE environment variable (see pom.xml's
# resource filtering). Not meant to be hand-edited after the jar is built - see TestModeLogger.
test-mode=${env.ENDERMAN_GRIEF_TEST_MODE}
