# Baked in at build time from the ENDERMAN_GRIEF_TEST_MODE environment variable (see
# build.gradle's processResources block). Not meant to be hand-edited after the jar is built -
# see TestModeLogger.
test-mode=${testMode}
